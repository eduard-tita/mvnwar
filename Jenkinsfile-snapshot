pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'mvn --batch-mode -V -U clean verify'
            }
        }
        stage('Deploy-snapshot') {
            steps {
                sh 'mvn --batch-mode -V -U deploy'
            }
        }
    }
}

